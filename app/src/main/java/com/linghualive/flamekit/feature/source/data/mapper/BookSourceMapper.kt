package com.linghualive.flamekit.feature.source.data.mapper

import com.linghualive.flamekit.core.database.entity.BookSourceEntity
import com.linghualive.flamekit.feature.source.domain.model.BookSource

fun BookSourceEntity.toDomain(): BookSource = BookSource(
    sourceUrl = sourceUrl,
    sourceName = sourceName,
    sourceGroup = sourceGroup,
    sourceType = sourceType,
    enabled = enabled,
    header = header,
    loginUrl = loginUrl,
    lastUpdateTime = lastUpdateTime,
    searchUrl = searchUrl,
    searchList = searchList,
    searchName = searchName,
    searchAuthor = searchAuthor,
    searchCover = searchCover,
    searchBookUrl = searchBookUrl,
    detailName = detailName,
    detailAuthor = detailAuthor,
    detailCover = detailCover,
    detailIntro = detailIntro,
    detailTocUrl = detailTocUrl,
    tocList = tocList,
    tocName = tocName,
    tocUrl = tocUrl,
    contentRule = contentRule,
    contentNextUrl = contentNextUrl,
    lastCheckTime = lastCheckTime,
    lastCheckSuccess = lastCheckSuccess,
)

fun BookSource.toEntity(): BookSourceEntity = BookSourceEntity(
    sourceUrl = sourceUrl,
    sourceName = sourceName,
    sourceGroup = sourceGroup,
    sourceType = sourceType,
    enabled = enabled,
    header = header,
    loginUrl = loginUrl,
    lastUpdateTime = lastUpdateTime,
    searchUrl = searchUrl,
    searchList = searchList,
    searchName = searchName,
    searchAuthor = searchAuthor,
    searchCover = searchCover,
    searchBookUrl = searchBookUrl,
    detailName = detailName,
    detailAuthor = detailAuthor,
    detailCover = detailCover,
    detailIntro = detailIntro,
    detailTocUrl = detailTocUrl,
    tocList = tocList,
    tocName = tocName,
    tocUrl = tocUrl,
    contentRule = contentRule,
    contentNextUrl = contentNextUrl,
    lastCheckTime = lastCheckTime,
    lastCheckSuccess = lastCheckSuccess,
)
